[build]
  # The command to build your React app
  command = "npm run build"
  
  # The folder where Vite puts the finished website
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

[dev]
  command = "npm run dev"